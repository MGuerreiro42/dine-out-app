export type Cuisine = {
  id: string;
  label: string;
  photo: string;
};

export type Occasion = {
  id: string;
  label: string;
  initial: string;
};

export type Ambient = {
  id: string;
  label: string;
};

export type Benefit = {
  text: string;
};
