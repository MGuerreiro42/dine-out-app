# Copy to .env.local (gitignored) and fill in a real value.

# Base URL for dine-out-backend-overture's REST API (see src/lib/apiClient.ts
# and src/mocks/repository.ts). Defaults to http://localhost:3000 when unset.
EXPO_PUBLIC_API_BASE_URL=http://localhost:3000
