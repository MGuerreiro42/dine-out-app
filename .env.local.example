# Copy to .env.local (gitignored) and fill in a real value.
#
# No env vars currently needed — EXPO_PUBLIC_GOOGLE_MAPS_API_KEY was removed
# once SearchMapView.tsx switched from react-native-maps to MapLibre (OSM
# raster tiles, no API key required).
